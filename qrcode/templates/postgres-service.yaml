apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: qr-db
  labels:
    app: postgresql
spec:
  clusterIP: None
  selector:
    app: postgresql
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres